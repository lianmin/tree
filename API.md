## API Report File for "@xtree/tree"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { TreeDataType } from './typings';
import { ValueType } from './typings';

// @public
class Tree<T = any, KV extends string = 'value', KC extends string = 'children'> {
    // Warning: (ae-forgotten-export) The symbol "ITreeConfig" needs to be exported by the entry point index.d.ts
    constructor(config?: ITreeConfig);
    children(node: TreeNode<TreeDataType<T, KV, KC>>): TreeNode<TreeDataType<T, KV, KC>>[];
    clear(): void;
    depth(node: TreeNode<TreeDataType<T, KV, KC>>): number;
    filter(predicate: PredicateFn<TreeDataType<T, KV, KC>>): TreeNode<TreeDataType<T, KV, KC>>[];
    // Warning: (ae-forgotten-export) The symbol "PredicateFn" needs to be exported by the entry point index.d.ts
    find(value: PredicateFn<TreeDataType<T, KV, KC>> | TreeDataType<T, KV, KC>[KV]): TreeNode<TreeDataType<T, KV, KC>> | undefined;
    flatten(node?: TreeNode<TreeDataType<T, KV, KC>>, parentPropName?: string): Array<T & {
        [key: string]: Extract<T, KV>;
    }>;
    format<P = any>(callback: (node: TreeNode<TreeDataType<T, KV, KC>>, children: P[] | undefined) => P): P[];
    // (undocumented)
    hash: string;
    height(node: TreeNode<TreeDataType<T, KV, KC>>): number;
    insertAfter(node: TreeNode<TreeDataType<T, KV, KC>>, siblingNode: TreeNode<TreeDataType<T, KV, KC>>): void;
    insertBefore(node: TreeNode<TreeDataType<T, KV, KC>>, siblingNode: TreeNode<TreeDataType<T, KV, KC>>): void;
    insertChild(node: TreeNode<TreeDataType<T, KV, KC>>, parentNode?: TreeNode<TreeDataType<T, KV, KC>>, pos?: 'leading' | 'trailing'): void;
    get isEmpty(): boolean;
    isFirstChild(node: TreeNode<TreeDataType<T, KV, KC>>): boolean;
    parents(node: TreeNode<TreeDataType<T, KV, KC>>): TreeNode<TreeDataType<T, KV, KC>>[];
    parse(data: TreeDataType<T, KV, KC>[]): void;
    parseDataToNode(data: TreeDataType<T, KV, KC>): TreeNode<TreeDataType<T, KV, KC>>;
    remove(node: TreeNode<TreeDataType<T, KV, KC>>): boolean;
    // (undocumented)
    root: TreeNode<TreeDataType<T, KV, KC>>;
    siblings(node: TreeNode<TreeDataType<T, KV, KC>>, pos?: 'left' | 'right' | 'all'): TreeNode<TreeDataType<T, KV, KC>>[];
    size(node?: TreeNode<TreeDataType<T, KV, KC>>): number;
    toData(): TreeDataType<T, KV, KC>[];
    // Warning: (ae-forgotten-export) The symbol "TraverseFn" needs to be exported by the entry point index.d.ts
    traverse(callback: TraverseFn<TreeDataType<T, KV, KC>>, order?: 'depth' | 'breadth'): void;
}
export default Tree;

export { TreeDataType }

// @public
export class TreeNode<T = Record<string, any>> {
    constructor(value: ValueType, originalData?: T);
    get isLeaf(): boolean;
    get isRoot(): boolean;
    left?: TreeNode<T>;
    originalData: T | undefined;
    parent?: TreeNode<T>;
    right?: TreeNode<T>;
    // (undocumented)
    static readonly ROOT_VALUE: ValueType;
    value: string | number | '__ROOT__';
}

export { ValueType }

// (No @packageDocumentation comment for this package)

```
